name: Verify generated curriculum mirror

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  parity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Rebuild generated HTML
        run: python3 scripts/build_site.py
      - name: Verify snapshot, links, files, and protection policy
        run: python3 scripts/verify_site.py
      - name: Require generated files to be committed
        run: git diff --exit-code -- index.html about.html parity.html lessons modules data/site-manifest.json data/public-links.json
